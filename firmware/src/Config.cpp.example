#include "Config.h"

// ---------------- WiFi credentials ----------------
const char *WIFI_SSID = "YOUR_WIFI_SSID";
const char *WIFI_PASSWORD = "YOUR_WIFI_PASSWORD";

// ---------------- WMATA API ----------------
const char *API_KEY = "YOUR_WMATA_API_KEY";

// ---------------- Station settings ----------------
const char *TrainStationCode = "A01";         // e.g. Metro Center
const int MinuteThreshold = 0;                // same as your Python Threshold arg
const unsigned long TrainRefreshTime = 30000; // 30s, in milliseconds

// ---------------- Abbreviation map ----------------
// Fill this in exactly like your Python Variables.AbrvStations dict.
std::map<String, String> AbrvStations = {
    {"Silver Spring", "Sil Spr"},
    {"Largo Town Center", "Largo"},
    {"Franconia-Springfield", "Frnc-Sprngfld"},
    // add the rest of your mappings here...
};
